//import { Constants } from "./Constants"

///// <reference path="Constants.ts" />

/**
 * The entry point to adding more logic through the mod (so stuff other than pure data such as items)
 * @param setupContext
 */
//export async function setup(setupContext: Modding.ModContext) {
//    const myConstants: Constants = await setupContext.loadModule('Constants.js');
//    console.log("If it worked then the following should be the namespace: " + myConstants.NAMESPACE);
//}